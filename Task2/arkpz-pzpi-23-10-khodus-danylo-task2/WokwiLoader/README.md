# 📦 Wokwi File Uploader

Консольное приложение на C# для автоматизации загрузки файлов в проекты [Wokwi](https://wokwi.com).

## ⚡ Быстрый старт

**Windows:**
```
Двойной клик по run.bat
```

**Любая ОС:**
```bash
cd WokwiLoader
dotnet run
```

📖 **Подробная инструкция:** [QUICK_START.md](QUICK_START.md)

---

## ✨ Возможности

- ✅ **Интерактивное создание конфигурации** - просто ответьте на вопросы при первом запуске
- ✅ **Автоматическое удаление** старых файлов из проекта Wokwi перед загрузкой
- ✅ **Рекурсивная загрузка** - обходит все вложенные папки
- ✅ **Сохранение структуры папок** - файлы загружаются с тем же путем
- ✅ **Поддержка бинарных файлов** - автоматическое Base64 кодирование (.bin, .hex, .png, .jpg, .wasm и др.)
- ✅ **Сохранение пути к папке** - не нужно вводить путь каждый раз
- ✅ **Режим отладки** - сохранение JSON запросов для диагностики
- ✅ **Детальная информация** - размер данных, количество файлов, статус загрузки
- ✅ **Не закрывается сразу** - ждет нажатия клавиши после завершения

---

## 🚀 Использование

### Первый запуск

1. Запустите `run.bat` (Windows) или `dotnet run` в папке WokwiLoader
2. Приложение попросит ввести:
   - **Project ID** (из URL вашего проекта на Wokwi)
   - **Project Name** (любое название)
   - **Unlisted** (публичный или скрытый проект)
   - **Cookie** (для аутентификации, опционально)
   - **Folder Path** (путь к папке с файлами, опционально)
3. Выберите папку с файлами для загрузки
4. Готово! Файлы загружены на Wokwi

### Последующие запуски

Конфигурация сохранена в `WokwiLoader/config.json` - больше ничего вводить не нужно!

Если вы сохранили путь к папке в конфиге, приложение спросит: "Использовать эту папку? (Y/n)"

---

## 🛠️ Полезные команды

### Windows (BAT файлы)

| Файл | Команда |
|------|---------|
| `run.bat` | Обычный запуск |
| `run-debug.bat` | Запуск с отладкой (сохраняет JSON) |
| `reset-config.bat` | Пересоздать конфигурацию |

### Командная строка

```bash
# Обычный запуск
dotnet run

# Режим отладки
dotnet run -- --debug
dotnet run -- -d

# Пересоздать конфигурацию
dotnet run -- --reset-config
dotnet run -- -r
```

---

## 📁 Структура проекта

```
WokwiLoader/
├── run.bat                    # Запуск (Windows)
├── run-debug.bat              # Запуск с отладкой
├── reset-config.bat           # Пересоздание конфига
├── README.md                  # Этот файл
├── QUICK_START.md             # Быстрая инструкция
└── WokwiLoader/               # Исходный код
    ├── Models/                # Модели данных
    │   ├── WokwiConfig.cs
    │   ├── WokwiFile.cs
    │   └── WokwiProject.cs
    ├── Program.cs             # Основная логика
    ├── config.example.json    # Пример конфигурации
    ├── config.json            # Ваша конфигурация (создается автоматически)
    └── README.md              # Техническая документация
```

---

## 🔧 Конфигурация (config.json)

Создается автоматически при первом запуске. Можно редактировать вручную:

```json
{
  "projectId": "449518275595938817",
  "projectName": "MyArduinoProject",
  "unlisted": false,
  "cookie": "session=abc123...",
  "folderPath": "E:\\Projects\\MyArduinoProject"
}
```

**Поля:**
- `projectId` (обязательно) - ID проекта из URL Wokwi
- `projectName` (обязательно) - Название проекта
- `unlisted` (по умолчанию `false`) - Скрыть проект от поиска
- `cookie` (опционально) - Cookie для аутентификации
- `folderPath` (опционально) - Путь к папке по умолчанию

---

## 📝 Получение необходимых данных

### Project ID

Найдите в URL вашего проекта:
```
https://wokwi.com/projects/449518275595938817
                          ^^^^^^^^^^^^^^^^^^
                          Это ваш Project ID
```

### Cookie (для приватных проектов)

**Chrome/Edge:**
1. Откройте https://wokwi.com и войдите
2. Нажмите `F12` → вкладка `Network`
3. Обновите страницу (`F5`)
4. Выберите любой запрос к `wokwi.com`
5. В `Request Headers` скопируйте значение `Cookie`

**Firefox:**
1. Откройте https://wokwi.com и войдите
2. Нажмите `F12` → вкладка `Сеть`
3. Обновите страницу (`F5`)
4. Выберите любой запрос к `wokwi.com`
5. В `Заголовки запроса` скопируйте `Cookie`

---

## 📂 Поддерживаемые файлы

### Текстовые файлы (UTF-8)
Загружаются как есть:
- Код: `.ino`, `.cpp`, `.c`, `.h`, `.hpp`, `.py`, `.js`, `.ts`
- Конфигурация: `.json`, `.toml`, `.xml`, `.yaml`
- Документация: `.txt`, `.md`

### Бинарные файлы (Base64)
Автоматически кодируются:
- Прошивки: `.bin`, `.hex`, `.elf`
- Изображения: `.png`, `.jpg`, `.jpeg`, `.gif`, `.bmp`, `.ico`
- Другое: `.wasm`, `.so`, `.dll`

---

## 💡 Примеры использования

### Простой проект Arduino

```
MyArduinoProject/
├── sketch.ino
├── diagram.json
└── wokwi.toml
```

Запустите приложение и укажите путь к `MyArduinoProject` - все 3 файла загрузятся.

### Проект с библиотеками

```
ComplexProject/
├── main.ino
├── diagram.json
├── lib/
│   ├── sensor.h
│   └── sensor.cpp
└── data/
    ├── logo.png
    └── config.json
```

Загрузится со структурой:
- `main.ino`
- `diagram.json`
- `lib/sensor.h`
- `lib/sensor.cpp`
- `data/logo.png` (в Base64)
- `data/config.json`

---

## 🔍 Как это работает

1. **Очистка проекта**
   - Отправляется POST на `https://wokwi.com/api/projects/save`
   - С пустым массивом файлов `{"files": [], ...}`
   - Удаляет все существующие файлы

2. **Сканирование папки**
   - Рекурсивно обходит все подпапки
   - Определяет бинарные файлы по расширению
   - Кодирует бинарные файлы в Base64

3. **Загрузка**
   - Отправляется POST с массивом файлов
   - Каждый файл: `{"name": "path/to/file", "content": "..."}`
   - Сохраняется структура папок

---

## ❓ Часто задаваемые вопросы

<details>
<summary><b>Обязательна ли Cookie?</b></summary>

Нет. Cookie нужна только для приватных проектов. Публичные проекты могут работать без нее.
</details>

<details>
<summary><b>Удаляются ли старые файлы?</b></summary>

Да! Приложение сначала удаляет ВСЕ файлы из проекта, затем загружает новые. Это гарантирует чистоту проекта.
</details>

<details>
<summary><b>Можно ли загрузить только новые файлы?</b></summary>

Нет. API Wokwi работает по принципу "замены всех файлов". Приложение всегда загружает полный набор файлов из папки.
</details>

<details>
<summary><b>Есть ли ограничения по размеру?</b></summary>

Да. Wokwi может иметь ограничения на размер проекта. При отправке >5MB приложение предупредит. Если получаете ошибку 500 - попробуйте загрузить меньше файлов.
</details>

<details>
<summary><b>Как пересоздать конфигурацию?</b></summary>

Запустите `reset-config.bat` или `dotnet run -- --reset-config`
</details>

---

## 🐛 Решение проблем

### Ошибка 500 Internal Server Error

**Причины:**
- Слишком большой размер данных (>5MB)
- Неверный Project ID
- Устаревший Cookie
- Нет прав доступа к проекту

**Решение:**
1. Запустите в режиме отладки: `run-debug.bat`
2. Проверьте размер в консоли
3. Проверьте Project ID в config.json
4. Обновите Cookie

### Ошибка 401 Unauthorized

Cookie устарела. Получите новую Cookie из браузера (см. выше).

### Ошибка 404 Not Found

Неверный Project ID. Проверьте ID в config.json.

### Приложение сразу закрывается

Старая версия. Пересоберите:
```bash
cd WokwiLoader
dotnet build
```

---

## 🛡️ Безопасность

⚠️ **Важно:** Файл `config.json` содержит вашу Cookie - **НЕ делитесь им** и **не коммитьте в Git**!

Файл `config.json` уже добавлен в `.gitignore`.

---

## 🔗 Технические детали

- **Язык:** C# (.NET 9.0)
- **API:** `https://wokwi.com/api/projects/save`
- **Метод:** POST
- **Формат:** JSON
- **Аутентификация:** Cookie-based
- **Кодировка:** UTF-8 для текста, Base64 для бинарных файлов

**Подробная техническая документация:** [WokwiLoader/README.md](WokwiLoader/README.md)

---

## 📜 Лицензия

MIT

---

## 🙏 Благодарности

Сделано для упрощения работы с [Wokwi](https://wokwi.com) - онлайн-симулятором Arduino и ESP32.

---

**Happy coding! 🎉**
