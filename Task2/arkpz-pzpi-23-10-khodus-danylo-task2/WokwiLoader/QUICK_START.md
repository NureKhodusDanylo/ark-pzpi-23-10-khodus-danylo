# 🚀 Быстрый старт Wokwi File Uploader

## Для нетерпеливых

1. **Запустите:** Двойной клик по `run.bat` (Windows) или `dotnet run` в папке WokwiLoader
2. **Введите данные проекта** (при первом запуске)
3. **Укажите папку** с файлами для загрузки
4. **Готово!** Файлы на Wokwi

---

## Первый запуск - что нужно знать

### 1️⃣ Получите Project ID

Откройте ваш проект на Wokwi и скопируйте ID из URL:

```
https://wokwi.com/projects/449518275595938817
                          ^^^^^^^^^^^^^^^^^^
                          Это ваш Project ID
```

### 2️⃣ Получите Cookie (для приватных проектов)

**Chrome/Edge:**
1. Откройте https://wokwi.com и войдите в аккаунт
2. Нажмите `F12`
3. Вкладка `Network` → обновите страницу `F5`
4. Выберите любой запрос к `wokwi.com`
5. Найдите `Request Headers` → скопируйте значение `Cookie`

**Firefox:**
1. Откройте https://wokwi.com и войдите в аккаунт
2. Нажмите `F12`
3. Вкладка `Сеть` → обновите страницу `F5`
4. Выберите любой запрос к `wokwi.com`
5. Найдите `Заголовки запроса` → скопируйте `Cookie`

### 3️⃣ Запустите приложение

**Windows (легкий способ):**
```
Двойной клик по run.bat
```

**Любая ОС:**
```bash
cd WokwiLoader
dotnet run
```

### 4️⃣ Ответьте на вопросы

При первом запуске приложение спросит:

```
1. Project ID: [вставьте ID из шага 1]
2. Project Name: [любое название, например "MyProject"]
3. Unlisted? (y/N): [нажмите Enter для публичного проекта]
4. Cookie: [вставьте Cookie из шага 2, или оставьте пустым]
5. Folder Path: [можно оставить пустым, потом выберете]
```

Все данные сохранятся в `config.json` - больше вводить не придется!

### 5️⃣ Выберите папку с файлами

```
Введите полный путь к папке: E:\MyProject\WokwiFiles
```

Приложение:
- Удалит все старые файлы из проекта Wokwi
- Загрузит все файлы из указанной папки (включая подпапки)
- Сохранит структуру папок

---

## Файлы для запуска (Windows)

| Файл | Описание |
|------|----------|
| `run.bat` | Обычный запуск |
| `run-debug.bat` | Запуск с отладкой (сохраняет JSON запросов) |
| `reset-config.bat` | Пересоздать конфигурацию |

---

## Часто задаваемые вопросы

### ❓ Можно ли не вводить Cookie?

Да, Cookie опциональна. Без нее могут работать публичные проекты, но для приватных она обязательна.

### ❓ Нужно ли каждый раз вводить путь к папке?

Нет! При первом выборе папки приложение предложит сохранить путь в конфиг. В следующий раз оно спросит: "Использовать эту папку? (Y/n)".

### ❓ Что будет с файлами, которые уже есть в проекте?

Приложение **сначала удаляет ВСЕ файлы** из проекта, затем загружает новые. Это гарантирует, что в проекте будут только те файлы, которые вы загрузили.

### ❓ Поддерживаются ли бинарные файлы?

Да! Бинарные файлы (.bin, .hex, .png, .jpg, .wasm и др.) автоматически кодируются в Base64.

### ❓ Можно ли загрузить вложенные папки?

Да! Приложение рекурсивно обходит все папки и сохраняет их структуру в Wokwi.

Пример:
```
MyProject/
├── main.ino
├── lib/
│   └── helper.h
└── data/
    └── config.json
```

В Wokwi будет:
- `main.ino`
- `lib/helper.h`
- `data/config.json`

### ❓ Как пересоздать конфигурацию?

Запустите `reset-config.bat` или:
```bash
dotnet run -- --reset-config
```

### ❓ Как включить режим отладки?

Запустите `run-debug.bat` или:
```bash
dotnet run -- --debug
```

Режим отладки сохраняет JSON запросов в файлы для диагностики проблем.

---

## Структура config.json

```json
{
  "projectId": "449518275595938817",
  "projectName": "MyArduinoProject",
  "unlisted": false,
  "cookie": "session=abc123...",
  "folderPath": "E:\\Projects\\MyArduinoProject"
}
```

**Примечания:**
- `projectId` - обязательное поле
- `projectName` - обязательное поле
- `unlisted` - `true` для скрытых проектов, `false` для публичных (по умолчанию)
- `cookie` - опционально, нужна для приватных проектов
- `folderPath` - опционально, путь по умолчанию (можно изменить при запуске)

---

## Решение проблем

### ⚠️ Ошибка 500 Internal Server Error

**Возможные причины:**
1. **Слишком большие файлы** - попробуйте загрузить меньше файлов
2. **Неверный Project ID** - проверьте ID в конфиге
3. **Устаревший Cookie** - получите новый Cookie из браузера
4. **Нет доступа к проекту** - убедитесь, что вы владелец проекта

**Решение:** Запустите в режиме отладки (`run-debug.bat`) и проверьте сохраненный JSON запрос.

### ⚠️ Ошибка 401 Unauthorized

Cookie устарела. Получите новую Cookie из браузера (см. шаг 2️⃣ выше).

### ⚠️ Ошибка 404 Not Found

Неверный Project ID. Проверьте URL вашего проекта на Wokwi.

### ⚠️ Приложение сразу закрывается

Теперь приложение всегда ждет нажатия клавиши. Если закрывается - пересоберите проект:
```bash
cd WokwiLoader
dotnet build
```

---

## Дополнительная информация

Полная документация: [README.md](WokwiLoader/README.md)

---

**Удачной загрузки! 🎉**
