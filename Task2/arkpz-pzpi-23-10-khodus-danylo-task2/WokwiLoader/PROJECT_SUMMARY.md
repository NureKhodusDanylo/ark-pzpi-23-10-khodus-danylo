# 📊 Wokwi File Uploader - Project Summary

## 🎯 Цель проекта

Автоматизация загрузки файлов в проекты Wokwi через REST API для упрощения работы с симулятором Arduino/ESP32.

---

## ✅ Реализованный функционал

### Основные возможности

- [x] **Интерактивное создание конфигурации** при первом запуске
- [x] **Автоматическое удаление** всех файлов из проекта перед загрузкой
- [x] **Рекурсивная загрузка** файлов из выбранной папки
- [x] **Поддержка вложенных папок** с сохранением структуры
- [x] **Автоматическое определение бинарных файлов** и Base64 кодирование
- [x] **Сохранение пути к папке** в конфиге для быстрого доступа
- [x] **Режим отладки** с сохранением JSON запросов
- [x] **Детальная информация** о процессе загрузки
- [x] **Обработка ошибок** с понятными сообщениями
- [x] **Ожидание нажатия клавиши** перед закрытием

### Технические особенности

- **Язык:** C# (.NET 9.0)
- **Архитектура:** Консольное приложение с моделями данных
- **API:** Wokwi REST API (`https://wokwi.com/api/projects/save`)
- **Аутентификация:** Cookie-based
- **Форматы файлов:** Текстовые (UTF-8) и бинарные (Base64)

---

## 📁 Структура проекта

```
WokwiLoader/
├── 📄 START_HERE.txt          # Краткая инструкция для начала работы
├── 📄 COMMANDS.txt             # Справка по командам
├── 📄 README.md                # Полная документация проекта
├── 📄 QUICK_START.md           # Подробная пошаговая инструкция
├── 📄 PROJECT_SUMMARY.md       # Этот файл - обзор проекта
│
├── 🔧 run.bat                  # Запуск приложения (Windows)
├── 🔧 run-debug.bat            # Запуск с отладкой (Windows)
├── 🔧 reset-config.bat         # Пересоздание конфигурации (Windows)
│
└── 📂 WokwiLoader/             # Исходный код приложения
    ├── 📂 Models/
    │   ├── WokwiConfig.cs      # Модель конфигурации
    │   ├── WokwiFile.cs        # Модель файла
    │   └── WokwiProject.cs     # Модель проекта
    │
    ├── Program.cs              # Основная логика приложения
    ├── WokwiLoader.csproj      # Файл проекта .NET
    ├── .gitignore              # Исключения для Git
    ├── config.example.json     # Пример конфигурации
    ├── config.json             # Ваша конфигурация (создается автоматически)
    └── README.md               # Техническая документация
```

---

## 🔧 Компоненты системы

### 1. Модели данных (Models/)

**WokwiConfig.cs**
- Конфигурация проекта
- Поля: ProjectId, ProjectName, Unlisted, Cookie, FolderPath

**WokwiFile.cs**
- Представление файла для загрузки
- Поля: Name, Content

**WokwiProject.cs**
- Полный проект для отправки на API
- Поля: Files, Id, Name, Unlisted

### 2. Основная логика (Program.cs)

**Методы:**
- `Main()` - точка входа, управление процессом
- `LoadConfig()` - загрузка или создание конфигурации
- `CreateConfigInteractively()` - интерактивное создание конфига
- `SaveConfig()` - сохранение конфигурации в JSON
- `LoadFilesRecursively()` - рекурсивное чтение файлов из папки
- `IsBinaryFile()` - определение типа файла
- `ClearProjectFiles()` - удаление всех файлов из проекта
- `UploadFiles()` - загрузка файлов на Wokwi
- `SendToWokwi()` - отправка HTTP POST запроса

### 3. BAT скрипты (для Windows)

**run.bat** - простой запуск приложения
```batch
@echo off
chcp 65001 > nul
cd /d "%~dp0WokwiLoader"
dotnet run
```

**run-debug.bat** - запуск с режимом отладки
```batch
@echo off
chcp 65001 > nul
cd /d "%~dp0WokwiLoader"
dotnet run -- --debug
```

**reset-config.bat** - пересоздание конфигурации
```batch
@echo off
chcp 65001 > nul
cd /d "%~dp0WokwiLoader"
dotnet run -- --reset-config
```

---

## 🚀 Использование

### Первый запуск

1. Запустите `run.bat` или `dotnet run`
2. Ответьте на вопросы для создания конфигурации:
   - Project ID (из URL проекта Wokwi)
   - Название проекта
   - Видимость проекта (public/unlisted)
   - Cookie для аутентификации
   - Путь к папке с файлами
3. Выберите папку с файлами для загрузки
4. Приложение загрузит файлы на Wokwi

### Последующие запуски

1. Запустите приложение
2. Подтвердите использование сохраненного пути или выберите новый
3. Файлы загрузятся автоматически

---

## 📊 Workflow диаграмма

```
┌─────────────────┐
│  Запуск         │
│  приложения     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐       ┌─────────────────┐
│ config.json     │  Нет  │  Интерактивное  │
│ существует?     ├──────►│  создание       │
└────────┬────────┘       │  конфигурации   │
         │ Да             └────────┬────────┘
         │                         │
         ▼                         │
┌─────────────────┐                │
│ Загрузка        │◄───────────────┘
│ конфигурации    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐       ┌─────────────────┐
│ Папка в         │  Да   │  Использовать   │
│ конфиге?        ├──────►│  эту папку?     │
└────────┬────────┘       └────────┬────────┘
         │ Нет                     │
         │                         ▼
         │               ┌─────────────────┐
         └──────────────►│  Выбор папки    │
                         │  пользователем  │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ 1. Удаление     │
                         │    всех файлов  │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ 2. Сканирование │
                         │    папки        │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │ 3. Загрузка     │
                         │    файлов       │
                         └────────┬────────┘
                                  │
                                  ▼
                         ┌─────────────────┐
                         │  Успех!         │
                         │  Ссылка на      │
                         │  проект         │
                         └─────────────────┘
```

---

## 🔐 Безопасность

### Защита конфиденциальных данных

- `config.json` добавлен в `.gitignore`
- Cookie не логируется в консоль
- Режим отладки сохраняет JSON локально (не передает)

### Рекомендации

- ⚠️ **НЕ делитесь** файлом `config.json` (содержит Cookie)
- ⚠️ **НЕ коммитьте** `config.json` в Git
- ⚠️ **Обновляйте Cookie** при истечении сессии

---

## 📈 Обработка ошибок

### HTTP 500 Internal Server Error
**Причины:**
- Слишком большой размер данных (>5MB)
- Неверный Project ID
- Устаревший Cookie
- Нет прав на проект

**Решение:** Используйте режим отладки для диагностики

### HTTP 401 Unauthorized
**Причина:** Устаревший Cookie

**Решение:** Получите новый Cookie из браузера

### HTTP 404 Not Found
**Причина:** Неверный Project ID

**Решение:** Проверьте ID в config.json

---

## 📝 Поддерживаемые файлы

### Текстовые (загружаются как UTF-8)
`.ino`, `.cpp`, `.c`, `.h`, `.hpp`, `.py`, `.js`, `.ts`, `.json`, `.toml`, `.xml`, `.yaml`, `.txt`, `.md`

### Бинарные (кодируются в Base64)
`.bin`, `.hex`, `.elf`, `.png`, `.jpg`, `.jpeg`, `.gif`, `.bmp`, `.ico`, `.wasm`, `.so`, `.dll`

---

## 🎓 Примеры использования

### Пример 1: Простой проект
```
MyArduinoProject/
├── sketch.ino
├── diagram.json
└── wokwi.toml
```

**Результат:** Все 3 файла загружаются в корень проекта Wokwi

### Пример 2: Проект с библиотеками
```
ComplexProject/
├── main.ino
├── diagram.json
├── lib/
│   ├── sensor.h
│   └── sensor.cpp
└── data/
    ├── logo.png
    └── config.json
```

**Результат:** Структура сохраняется:
- `main.ino`
- `diagram.json`
- `lib/sensor.h`
- `lib/sensor.cpp`
- `data/logo.png` (Base64)
- `data/config.json`

---

## 🔄 API взаимодействие

### Endpoint
```
POST https://wokwi.com/api/projects/save
```

### Headers
```
Content-Type: application/json
Cookie: <ваш cookie>
```

### Request Body (очистка проекта)
```json
{
  "files": [],
  "id": "449518275595938817",
  "name": "MyProject",
  "unlisted": false
}
```

### Request Body (загрузка файлов)
```json
{
  "files": [
    {
      "name": "sketch.ino",
      "content": "void setup() {...}"
    },
    {
      "name": "data/image.png",
      "content": "iVBORw0KGgoAAAANSUhEUg..."
    }
  ],
  "id": "449518275595938817",
  "name": "MyProject",
  "unlisted": false
}
```

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| START_HERE.txt | Краткая инструкция для быстрого старта |
| COMMANDS.txt | Справка по доступным командам |
| QUICK_START.md | Подробная пошаговая инструкция |
| README.md (корень) | Полная документация проекта |
| WokwiLoader/README.md | Техническая документация кода |
| PROJECT_SUMMARY.md | Этот файл - обзор проекта |

---

## 🛠️ Системные требования

- **.NET SDK 9.0** или выше
- **Windows/Linux/MacOS**
- **Интернет-соединение** для доступа к Wokwi API
- **Аккаунт Wokwi** (для приватных проектов)

---

## ✅ Тестирование

### Проверка сборки
```bash
cd WokwiLoader
dotnet build
```

### Проверка Debug сборки
```bash
dotnet build -c Debug
```

### Проверка Release сборки
```bash
dotnet build -c Release
```

### Очистка и пересборка
```bash
dotnet clean
dotnet build
```

---

## 📦 Сборка Release версии

```bash
cd WokwiLoader
dotnet publish -c Release -r win-x64 --self-contained
```

**Результат:** Готовый к распространению `.exe` файл в:
```
WokwiLoader/bin/Release/net9.0/win-x64/publish/WokwiLoader.exe
```

---

## 🚧 Известные ограничения

1. **Размер проекта:** Wokwi может ограничивать размер проекта (~5MB)
2. **Cookie lifetime:** Cookie имеет срок действия, требуется периодическое обновление
3. **Только замена:** API не поддерживает частичное обновление, только полную замену файлов
4. **Формат файлов:** Бинарные файлы должны быть в списке расширений для Base64 кодирования

---

## 🔮 Возможные улучшения

- [ ] GUI интерфейс (WPF/Avalonia)
- [ ] Мониторинг папки и автоматическая загрузка при изменениях
- [ ] История загрузок
- [ ] Поддержка нескольких проектов
- [ ] Compression для больших файлов
- [ ] Синхронизация в обе стороны (скачивание + загрузка)
- [ ] Интеграция с CI/CD

---

## 📄 Лицензия

MIT License - свободное использование, модификация и распространение

---

## 👨‍💻 Автор

Создано для упрощения работы с [Wokwi](https://wokwi.com) - онлайн-симулятором Arduino и ESP32

---

**Дата создания:** 2025-12-06
**Версия:** 1.0.0
**Статус:** ✅ Готово к использованию
